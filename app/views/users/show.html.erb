<% provide(:title, @user.name) %>
<div class="row">
  <aside class="span4">
    <section>
      <h1>
        <!--
        <%= gravatar_for @user, size: 40 %>
        -->
        <%= image_tag(@user.avatar_url) if @user.avatar? %>
        <%= @user.name %>
      </h1>
    </section>
  </aside>
</div>